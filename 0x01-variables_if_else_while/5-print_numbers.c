#include <stdlib.h>
#include <stdio.h>

/**
 * main - Determines if a number is positive, negative or zero.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0

	for (; i <= 9; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
